# Rainfall Project SAIL — FastAPI Backend

## Overview
This FastAPI service stores user queries, accepts agent-posted daily/monthly forecasts,
and returns PNG plots (image/png) for the frontend to display.

## Run locally
1. Install dependencies:
